name: CI (Vite) on Self-Hosted

on:
  push:
    branches: [ main ]

jobs:
  build-and-restart:
    runs-on: self-hosted  # ตรงกับ label ของ runner คุณ

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install deps (npm ci)
        run: yarn ci

      - name: Build (Vite)
        env:
          NODE_ENV: production
          CI: true
        run: yarn run build  # ผลลัพธ์ที่ dist/
